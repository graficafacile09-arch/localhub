/**
 * LocalHub Brain — Orchestrator
 *
 * Punto di accesso al modulo orchestratore.
 * L'orchestratore coordina tutti i sottosistemi di Brain
 * (builder, retriever, ranking, reasoning, memory) in un unico flusso.
 *
 * @module lib/brain/orchestrator
 */

export type { BrainOrchestrator } from "./brain-orchestrator";
